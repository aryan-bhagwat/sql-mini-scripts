## Query: Customer Lifetime Value (CLV)

**Purpose:** Find total revenue contributed by each customer.

**Key Concepts:**
- `SUM(order_total)` for lifetime spend
- `GROUP BY` customer
- Crucial metric for customer segmentation and retention
