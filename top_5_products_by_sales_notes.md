## Query: Top 5 Products by Sales Revenue

**Purpose:** Identify the 5 highest revenue-generating products.

**Key Concepts:**
- Revenue = quantity × unit price
- `JOIN` with product info
- `GROUP BY` product
- `ORDER BY DESC` + `LIMIT` for top N
